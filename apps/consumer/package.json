{
  "name": "consumer",
  "description": "Consumer is a simple Node.js app that is gonna consume slow processes. so slow he might fall asleep.",
  "version": "0.0.0",
  "author": "Yakkyofy Team",
  "scripts": {
    "start": "tsc && node dist/app.js",
    "build": "tsc",
    "dev": "nodemon src/app.ts"
  },
  "dependencies": {
    "@pandarocket/models": "*",
    "dotenv": "^16.3.1",
    "rabbitmq-client": "^4.3.0"
  },
  "devDependencies": {
    "@pandarocket/types": "*",
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
